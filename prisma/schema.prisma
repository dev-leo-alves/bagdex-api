generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "sqlite"
    url      = "file:./bagdex-api.db"
}

model Tier {
    id   Int    @id @default(autoincrement())
    name String @unique
    url  String @unique

    @@map("tier")
}
