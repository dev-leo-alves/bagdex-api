generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model Tier {
    id   Int    @id
    name String @unique
    url  String @unique

    @@map("tiers")
}
