generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model Tier {
    id   Int    @id @default(autoincrement())
    name String @unique
    url  String @unique

    @@map("tier")
}
